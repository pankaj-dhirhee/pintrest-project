<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
</head>
<body>
    <h1>Login Successful!</h1>
    <script>
        // JavaScript to open a new tab with the '/profile' route and close the old one
        function openProfileInNewTab() {
            // Open '/profile' route in a new tab
            
              const newTab = location.href = '/profile';
              //window.open("/profile")
            // Close the current tab
            window.close();
            // Prevent the new tab from being blocked by the browser's popup blocker
             newTab.focus();
        }

        // Call the function when the page loads
        window.onload = openProfileInNewTab;
    </script>
</body>
</html>
